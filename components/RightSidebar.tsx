import React from 'react'

const RightSidebar = ({ user, transactions, banks }) => {
  return (
    <aside className="right-sidebar">
      <section className="flex flex-col pb-8"></section>
    </aside>
  )
}

export default RightSidebar